<resources>
    <!-- Base application theme -->
    <style name="Theme.NavigationUm" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Customize your theme here -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryDark</item>
        <item name="colorOnPrimary">@color/colorAccent</item>
        <item name="colorSecondary">@color/colorSecondary</item>
    </style>

    <!-- AppBarOverlay style -->
    <style name="Theme.NavigationUm.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <!-- Customize AppBar colors, fonts, etc. -->
    </style>

    <!-- PopupOverlay style -->
    <style name="Theme.NavigationUm.PopupOverlay" parent="ThemeOverlay.AppCompat.Light">
        <!-- Customize Popup colors, fonts, etc. -->
    </style>
</resources>
